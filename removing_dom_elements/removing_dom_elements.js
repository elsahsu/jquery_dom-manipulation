/*
Remove the <ul> from the first article item!

Must use jQuery's remove() method.
*/

// Start with this variable!
var articleItems;

articleItems = $('h3').siblings('ul').remove();

//$( ".hello" ).remove();
//$( "div" ).remove( ".hello" );
//Sample Image Title here
